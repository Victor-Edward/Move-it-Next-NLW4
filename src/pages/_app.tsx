import '../styles/global.css'

import { ChallengeProvider, ChallengesContext } from '../context/ChallengeContext'

function MyApp({ Component, pageProps }) {


  return (
      <Component {...pageProps} />
    )
  }

export default MyApp
